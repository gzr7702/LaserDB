The Laser Database

Service Orders:

This is a database to track the Service Order forms completed in the field by engineers
as well as parts that they use.

Using Python 3.4 and Django 1.7, and Bootstrap

To do:

TESTING:
- form testing in service_orders
- refactor model tests to use data object
- add functional tests

FRONT END:
- change SO form to only display unused parts
- Display of SO's not showing parts
- add error message for bad forms
- Add links to add extra customer, machines, addresses, and parts from the larger forms?

OTHER:
- Notes field on Info form should not be required
- Add search for RMA, Part serial number, Engineer
- Add API
